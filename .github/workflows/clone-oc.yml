name: clone OC

on:
  workflow_dispatch:

jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
        with:
          repository: "OC-Git/Konfigurator2"
          token: ${{secrets.PAT}}
          path: "Konfigurator2"
          
      - run: ls
