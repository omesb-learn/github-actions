name: clone WW

on:
  workflow_dispatch:

jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      with:
        repository: "omesb/dots"
        ssh-key: ${{secrets.SSH_PUB}}
    - run: ls
